# Server
DATABASE_URL=postgresql://summarizer:p.summarizer@postgres:5432/summarizer
JWT_SECRET=
APP_URL=http://localhost:9090

#DB
POSTGRES_PASSWORD=p.summarizer
POSTGRES_USER=summarizer
POSTGRES_DB=summarizer

# Managed LLM keys (used for subscription mode)
MANAGED_PROVIDER=groq # groq|gemini
GROQ_API_KEY=
GEMINI_API_KEY=
DEFAULT_MODEL=llama-3.1-8b-instant

# Usage limits
FREE_DAILY_LIMIT=5
PAID_MONTHLY_LIMIT=500

# Extension (anonymous telemetry + free mode)
# - Used by /api/admin/stats
ADMIN_STATS_TOKEN=
# - Daily free summarize limit per installId (anonymous)
EXTENSION_DAILY_LIMIT=5
